# 影子功能测试报告

**测试时间**：2026-09-07
**测试范围**：断联日记 NOEX · 「影子」功能（yingzi 页面 + shadow 引擎）
**测试套件**：`miniprogram/_test/` 下 3 套共 135 项断言
**总体结果**：✅ **135 / 135 通过**（修复 1 项后）

---

## 一、shadow 引擎单元测试（[shadow] 组）

位于 `miniprogram/_test/wx-test.js` line 92-114，共 7 项，全过。

| # | 断言 | 结果 |
|---|---|---|
| 1 | `analyzeChat` 消息计数 ≥ 4 | ✅ |
| 2 | `analyzeChat` 口头禅 top 识别「嗯嗯」 | ✅ |
| 3 | `analyzeChat` 反问率 qRate > 0 | ✅ |
| 4 | `shadowReply`「想你」→ 命中 miss pool | ✅ |
| 5 | `shadowReply`「为什么」→ 命中 ask pool | ✅ |
| 6 | `shadowReply` 无关输入 → fallback pool | ✅ |
| 7 | `profileSummary` 至少输出 1 行画像文字 | ✅ |

**引擎结论**：✅ 5 种情绪分支（miss / ask / angry / bye / greet）+ fallback 都覆盖，画画像功能正常。

---

## 二、yingzi 页面端到端测试（`[yingzi]` 区段）

位于 `miniprogram/_test/e2e-test.js` line 220-258，共 13 项，全过。

| 阶段 | 断言 | 结果 |
|---|---|---|
| **进入** | onShow → 默认 view 模式 | ✅ |
| | shName 默认空 | ✅ |
| | 切到 setup 后回 view（onCancelSetup） | ✅ |
| **录入** | onNameInput 设置 shName | ✅ |
| | onLogInput 设置 shLog（≥ 100 字） | ✅ |
| **分析** | onGoAnalyze 切到 analyze 模式 | ✅ |
| | 启用后 canEnter = true | ✅ |
| **聊天** | onEnterChat 切到 chat | ✅ |
| | onChatSend me 消息进入 msgs | ✅ |
| | 影子回复已生成（ai 消息存在） | ✅ |
| **结束** | onSessionEndChat 回 view | ✅ |
| | onSessionEndToGuardian 触发 switchTab 到 /pages/xiaobai/xiaobai | ✅ |

**页面结论**：✅ view → setup → analyze → view（启用）→ chat → view 全链路流畅。

---

## 三、相关边界场景（5 / 5 全过）

| 场景 | 来源 | 结果 |
|---|---|---|
| yingzi 日志太短（< 20 字）→ toast 提示且不进 analyze | e2e #2 | ✅ |
| yingzi 还没 pickFile 就 onPickSpeaker → 不崩 | e2e #11 | ✅ |
| 影子 session end → 自动转跳 /pages/xiaobai/xiaobai | e2e yingzi 区段 | ✅ |
| 危机词触发 → 显示热线 400-161-9995 | e2e #8 | ✅ |
| shadow 池稳定：24 条尾巴 30 次采样必中至少 1 次 | e2e #7 | ✅ |

---

## 四、其他引擎层（影子不是孤岛，依赖它）

- **storage**（5/5 ✅）：默认状态、读写 roundtrip、字段缺失容错、clear
- **guardian**（14/14 ✅）：5 种 intent 检测、4 种人格（warm/sharp/tsun/spoil）、crisis 兜底、ENCOURAGE 配 1/3/7/21 天
- **wechat**（8/8 ✅）：UTF-8 BOM 解码、UTF-16LE 启发式、3 种聊天头部解析、默认说话人推断

---

## 五、本次修复

| 文件 | 行 | 旧值 | 新值 | 原因 |
|---|---|---|---|---|
| `miniprogram/_test/integration-test.js` | 145-149 | sharp 20 次采样 | sharp 30 次采样 | 毒舌池从 8→24 条后，20 次命中概率降到 ~33%，偶发 flaky；与 e2e 同步提到 30 次 |

---

## 六、跑测试命令

```bash
cd "C:\Users\XIAOMI\Desktop\no contact\no-contact\miniprogram"
node _test/wx-test.js
node _test/integration-test.js
node _test/e2e-test.js
```

或一行：
```bash
cd /c/Users/XIAOMI/Desktop/no\ contact/no-contact/miniprogram && node _test/wx-test.js && node _test/integration-test.js && node _test/e2e-test.js
```

---

## 七、结论

> **影子功能可以放心交付** —— 引擎 + 页面全链路 + 边界都覆盖了；唯一一项 flaky 是测试断言陈旧所致，已修。
